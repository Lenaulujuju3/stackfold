# StackFold — Full Site (Screenshot-Themed, Functional MVP)

**Includes** pages, protected dashboard, mocked forms, NextAuth credentials login.

## Run
```
npm install
cp .env.example .env.local
npm run dev
```

## Build/Deploy
```
npm run build
npm i -g vercel
vercel --prod --confirm --name stackfold
```
