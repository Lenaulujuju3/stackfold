import { getServerSession } from 'next-auth';import Link from 'next/link';
export default async function Dashboard(){const session=await getServerSession();if(!session){return(<section className='container py-12'><div className='card'><div className='font-semibold'>Please log in</div><p className='text-gray-300 mt-1'>This area is protected. <Link className='btn btn-solid mt-3' href='/login'>Login</Link></p></div></section>)}const cards=['Leads Velocity','Reachability','Demand Index','Risk Index','Senior Opportunity','Premium Propensity'];return(<section className='container py-12'><h1 className='text-2xl font-bold mb-4'>Dashboard</h1><div className='grid md:grid-cols-3 gap-4'>{cards.map((t,i)=>(<div key={i} className='card'><div className='font-semibold'>{t}</div><div className='mt-3 h-28 rounded-xl border border-white/10 bg-white/5'/></div>))}</div></section>)}